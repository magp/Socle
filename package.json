{
  "name": "socle",
  "version": "0.2.5",
  "type": "module",
  "bin": {
    "socle": "./cli/index.js"
  },
  "scripts": {
    "build": "node reference-app/utils/build.js",
    "test": "vitest run && playwright test --config reference-app/playwright.config.js",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test --config reference-app/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "fake-indexeddb": "^6.2.5",
    "happy-dom": "^20.9.0",
    "vitest": "^4.1.0"
  }
}
