export function reducer(state, event) {
  switch (event.type) {
    // Add your event handlers here:
    // case 'thing:created':
    //   return { ...state, things: [...(state.things ?? []), event.payload] };
    default:
      return state;
  }
}
